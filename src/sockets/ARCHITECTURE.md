# Socket Handlers Architecture

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Frontend Clients                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │   Browser    │  │   Browser    │  │   Browser    │              │
│  │   Client 1   │  │   Client 2   │  │   Client 3   │              │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘              │
│         │                  │                  │                       │
│         └──────────────────┴──────────────────┘                      │
│                            │                                          │
│                    Socket.io Client                                  │
└────────────────────────────┼────────────────────────────────────────┘
                             │
                    WebSocket Connection
                             │
┌────────────────────────────┼────────────────────────────────────────┐
│                    Socket.io Server                                  │
│                            │                                          │
│  ┌─────────────────────────┴──────────────────────────────┐         │
│  │          src/configs/socket.js (Main Config)            │         │
│  │                                                          │         │
│  │  ┌────────────────────────────────────────────────┐    │         │
│  │  │  initSocket(server)                            │    │         │
│  │  │  - Create Socket.io server                     │    │         │
│  │  │  - Initialize Redis adapter (optional)         │    │         │
│  │  │  - Initialize modular handlers                 │    │         │
│  │  │  - Set up legacy event handlers                │    │         │
│  │  └────────────────────────────────────────────────┘    │         │
│  └────────────┬─────────────────────────┬─────────────────┘         │
│               │                         │                            │
│               ▼                         ▼                            │
│  ┌────────────────────────┐  ┌──────────────────────────┐          │
│  │  TicketCollisionHandler│  │  TimelineSocketHandler   │          │
│  │  (ticketCollision.     │  │  (timeline.socket.js)    │          │
│  │   socket.js)           │  │                          │          │
│  │                        │  │                          │          │
│  │  • Track viewers       │  │  • Track subscribers     │          │
│  │  • Typing indicators   │  │  • Broadcast events      │          │
│  │  • Collision warnings  │  │  • Room management       │          │
│  │  • Cleanup stale       │  │  • Stats tracking        │          │
│  │                        │  │                          │          │
│  └────────────┬───────────┘  └────────────┬─────────────┘          │
│               │                           │                         │
└───────────────┼───────────────────────────┼─────────────────────────┘
                │                           │
                │                           │
    ┌───────────┴──────────┐    ┌──────────┴─────────────┐
    │  Socket Rooms        │    │  Socket Rooms          │
    │                      │    │                        │
    │  ticket:123          │    │  timeline:client:456   │
    │  ticket:456          │    │  timeline:case:789     │
    │  ticket:789          │    │  timeline:lead:101     │
    └──────────────────────┘    └────────────────────────┘
                │                           │
                └───────────┬───────────────┘
                            │
┌───────────────────────────┼────────────────────────────────────────┐
│                    Backend Services                                 │
│                            │                                         │
│  ┌────────────────────────┴───────────────────────────┐            │
│  │  Services Use Handlers via Getter Functions        │            │
│  └─────────────────────────────────────────────────────┘            │
│                                                                      │
│  ┌──────────────────┐  ┌─────────────────┐  ┌──────────────────┐  │
│  │  CRM Activity    │  │  Case Service   │  │  Invoice Service │  │
│  │  Service         │  │                 │  │                  │  │
│  │                  │  │                 │  │                  │  │
│  │  getTimeline     │  │  getTimeline    │  │  getTimeline     │  │
│  │  Handler()       │  │  Handler()      │  │  Handler()       │  │
│  │    .broadcast    │  │    .broadcast   │  │    .broadcast    │  │
│  │     NewItem()    │  │     NewItem()   │  │     NewItem()    │  │
│  └──────────────────┘  └─────────────────┘  └──────────────────┘  │
│                                                                      │
│  ┌──────────────────┐  ┌─────────────────┐                         │
│  │  Conversation    │  │  Support Ticket │                         │
│  │  Service         │  │  Service        │                         │
│  │                  │  │                 │                         │
│  │  getTicket       │  │  getTicket      │                         │
│  │  Collision       │  │  Collision      │                         │
│  │  Handler()       │  │  Handler()      │                         │
│  │    .getViewers() │  │    .getViewers()│                         │
│  └──────────────────┘  └─────────────────┘                         │
└─────────────────────────────────────────────────────────────────────┘
```

## Handler Lifecycle

```
┌─────────────────────────────────────────────────────────────────┐
│                    Server Startup                                │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
           ┌───────────────────────────────┐
           │  initSocket(server)           │
           │  in src/configs/socket.js     │
           └───────────┬───────────────────┘
                       │
           ┌───────────┴────────────┐
           │                        │
           ▼                        ▼
  ┌─────────────────┐      ┌────────────────┐
  │ new Ticket      │      │ new Timeline   │
  │ Collision       │      │ Socket         │
  │ Handler(io)     │      │ Handler(io)    │
  └────────┬────────┘      └────────┬───────┘
           │                        │
           ▼                        ▼
  ┌─────────────────┐      ┌────────────────┐
  │ handler.        │      │ handler.       │
  │ initialize()    │      │ initialize()   │
  └────────┬────────┘      └────────┬───────┘
           │                        │
           └───────────┬────────────┘
                       │
                       ▼
           ┌───────────────────────────────┐
           │  Handlers listen for socket   │
           │  'connection' events          │
           └───────────┬───────────────────┘
                       │
                       ▼
           ┌───────────────────────────────┐
           │  Client connects              │
           └───────────┬───────────────────┘
                       │
           ┌───────────┴────────────┐
           │                        │
           ▼                        ▼
  ┌─────────────────┐      ┌────────────────┐
  │ Listen for:     │      │ Listen for:    │
  │ - ticket:view   │      │ - timeline:    │
  │ - ticket:typing │      │   subscribe    │
  │ - ticket:leave  │      │ - timeline:    │
  │ - disconnect    │      │   unsubscribe  │
  └────────┬────────┘      └────────┬───────┘
           │                        │
           ▼                        ▼
  ┌─────────────────┐      ┌────────────────┐
  │ Handle events   │      │ Handle events  │
  │ Update state    │      │ Update state   │
  │ Emit responses  │      │ Emit responses │
  └─────────────────┘      └────────────────┘
           │                        │
           └───────────┬────────────┘
                       │
                       ▼
           ┌───────────────────────────────┐
           │  Server Shutdown Signal       │
           └───────────┬───────────────────┘
                       │
                       ▼
           ┌───────────────────────────────┐
           │  shutdownSocket()             │
           └───────────┬───────────────────┘
                       │
           ┌───────────┴────────────┐
           │                        │
           ▼                        ▼
  ┌─────────────────┐      ┌────────────────┐
  │ handler.        │      │ handler.       │
  │ shutdown()      │      │ shutdown()     │
  │ - Clear timers  │      │ - Clear state  │
  │ - Clear state   │      │                │
  └─────────────────┘      └────────────────┘
```

## Ticket Collision Detection Flow

```
┌──────────────┐
│ Agent 1      │
│ Opens Ticket │
└──────┬───────┘
       │
       │ emit('ticket:view', { ticketId, agentId, agentName })
       │
       ▼
┌────────────────────────────────────────────────────────┐
│  TicketCollisionHandler.onAgentViewTicket()            │
│                                                         │
│  1. Create viewer entry in activeViewers Map           │
│  2. Join socket room: ticket:{ticketId}                │
│  3. Check for existing viewers                         │
│     - If empty: No collision                           │
│     - If others exist: Emit collision warning          │
│                                                         │
│  activeViewers.set(ticketId, new Map([                 │
│    [agentId, {                                         │
│      agentName: 'Agent 1',                             │
│      status: 'viewing',                                │
│      since: Date.now(),                                │
│      socketId: 'abc123'                                │
│    }]                                                  │
│  ]))                                                   │
└────────────────────────┬───────────────────────────────┘
                         │
                         │ emit('viewers:updated', { viewers: [...] })
                         │
                         ▼
                  ┌──────────────┐
                  │ Agent 1      │
                  │ (No warning) │
                  └──────────────┘
                         │
                         │ (Time passes)
                         │
┌──────────────┐         │
│ Agent 2      │         │
│ Opens Same   │         │
│ Ticket       │         │
└──────┬───────┘         │
       │                 │
       │ emit('ticket:view', { ticketId, agentId2, agentName2 })
       │                 │
       ▼                 │
┌────────────────────────┴───────────────────────────────┐
│  TicketCollisionHandler.onAgentViewTicket()            │
│                                                         │
│  1. Check existing viewers (Agent 1 found!)            │
│  2. Join socket room: ticket:{ticketId}                │
│  3. Emit collision warning to Agent 2                  │
│  4. Emit agent:joined to Agent 1                       │
│  5. Emit viewers:updated to both                       │
│                                                         │
│  activeViewers.set(ticketId, new Map([                 │
│    [agentId1, { ... }],                                │
│    [agentId2, {                                        │
│      agentName: 'Agent 2',                             │
│      status: 'viewing',                                │
│      since: Date.now(),                                │
│      socketId: 'def456'                                │
│    }]                                                  │
│  ]))                                                   │
└────────────┬────────────────────┬──────────────────────┘
             │                    │
             │                    │ emit('collision:warning')
             │                    │
       emit('agent:joined')       ▼
             │              ┌──────────────┐
             │              │ Agent 2      │
             │              │ ⚠️ Warning!  │
             │              │ Agent 1 is   │
             │              │ viewing      │
             ▼              └──────────────┘
       ┌──────────────┐
       │ Agent 1      │
       │ 🔔 Agent 2   │
       │ joined       │
       └──────────────┘
             │
             │ Agent 1 starts typing
             │
             │ emit('ticket:typing', { ticketId, agentId1 })
             │
             ▼
┌────────────────────────────────────────────────────────┐
│  TicketCollisionHandler.onAgentTyping()                │
│                                                         │
│  1. Update viewer status to 'typing'                   │
│  2. Emit agent:typing to others in room (Agent 2)      │
│                                                         │
│  activeViewers.get(ticketId).set(agentId1, {           │
│    status: 'typing',  // ← Changed                     │
│    typingStartedAt: Date.now()                         │
│  })                                                    │
└────────────────────────┬───────────────────────────────┘
                         │
                         │ emit('agent:typing', {
                         │   warning: 'COLLISION_RISK',
                         │   message: 'Agent 1 is typing...'
                         │ })
                         │
                         ▼
                  ┌──────────────┐
                  │ Agent 2      │
                  │ 🚨 CRITICAL  │
                  │ Agent 1 is   │
                  │ typing a     │
                  │ response!    │
                  └──────────────┘
```

## Timeline Update Flow

```
┌──────────────┐
│ Frontend     │
│ Component    │
└──────┬───────┘
       │
       │ emit('timeline:subscribe', {
       │   entityType: 'client',
       │   entityId: '123'
       │ })
       │
       ▼
┌────────────────────────────────────────────────────────┐
│  TimelineSocketHandler.onSubscribe()                   │
│                                                         │
│  1. Join socket room: timeline:client:123              │
│  2. Track subscription in socketSubscriptions          │
│  3. Increment subscriber count in entitySubscribers    │
│  4. Emit subscription confirmation                     │
│                                                         │
│  socketSubscriptions.set(socketId, new Set([           │
│    'client:123'                                        │
│  ]))                                                   │
│                                                         │
│  entitySubscribers.set('client:123', 1)                │
└────────────────────────┬───────────────────────────────┘
                         │
                         │ emit('timeline:subscribed')
                         │
                         ▼
                  ┌──────────────┐
                  │ Frontend     │
                  │ Subscribed ✓ │
                  └──────────────┘
                         │
                         │ (Time passes)
                         │
┌──────────────┐         │
│ Backend      │         │
│ Service      │         │
│ Creates New  │         │
│ Activity     │         │
└──────┬───────┘         │
       │                 │
       │ const handler = getTimelineHandler();
       │ handler.broadcastNewItem('client', '123', {
       │   id: 'activity_456',
       │   type: 'activity',
       │   title: 'New call logged'
       │ });
       │
       ▼
┌────────────────────────────────────────────────────────┐
│  TimelineSocketHandler.broadcastNewItem()              │
│                                                         │
│  1. Check if entity has subscribers                    │
│     entitySubscribers.has('client:123') → true         │
│                                                         │
│  2. Emit to room: timeline:client:123                  │
│     io.to('timeline:client:123')                       │
│       .emit('timeline:new_item', { item: {...} })      │
└────────────────────────┬───────────────────────────────┘
                         │
                         │ emit('timeline:new_item')
                         │
                         ▼
                  ┌──────────────┐
                  │ Frontend     │
                  │ Receives     │
                  │ New Item     │
                  │ → Updates UI │
                  └──────────────┘
```

## Data Flow Summary

```
┌─────────────────────────────────────────────────────────────────┐
│                         DATA FLOW                                │
└─────────────────────────────────────────────────────────────────┘

1. CLIENT → SERVER (Events)
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   • ticket:view          → Join ticket room, track viewer
   • ticket:typing        → Update status, warn others
   • ticket:stop_typing   → Update status, notify others
   • ticket:leave         → Leave room, cleanup
   • timeline:subscribe   → Join entity timeline room
   • timeline:unsubscribe → Leave entity timeline room

2. SERVER STATE (Maps)
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   • activeViewers        → ticketId → Map(agentId → viewerData)
   • socketToTickets      → socketId → Set(ticketIds)
   • socketSubscriptions  → socketId → Set(entityKeys)
   • entitySubscribers    → entityKey → subscriberCount

3. SERVER → CLIENT (Events)
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   • collision:warning    → Warn about other viewers
   • viewers:updated      → Current viewer list
   • agent:typing         → Typing indicator
   • agent:joined         → New viewer notification
   • timeline:new_item    → New timeline item
   • timeline:item_updated → Item update
   • timeline:refresh     → Full refresh needed

4. BACKEND SERVICES
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   Service → getHandler() → broadcastUpdate() → WebSocket → Client
```

## Cleanup & Resource Management

```
┌─────────────────────────────────────────────────────────────────┐
│                     CLEANUP MECHANISMS                           │
└─────────────────────────────────────────────────────────────────┘

1. DISCONNECT CLEANUP
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   socket.on('disconnect') →
     - Find all tickets/timelines for socketId
     - Remove from activeViewers/subscriptions
     - Notify remaining users
     - Cleanup empty Maps

2. STALE VIEWER CLEANUP (Every 5 minutes)
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   setInterval(cleanupStaleViewers, 5 * 60 * 1000) →
     - Check viewer.since timestamps
     - Remove viewers older than 30 minutes
     - Emit agent:left events
     - Update viewers:updated

3. GRACEFUL SHUTDOWN
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   shutdownSocket() →
     - handler.shutdown() for all handlers
       - Clear intervals
       - Clear Maps
       - Log shutdown
     - Close Socket.io server
     - Close Redis connections
```

## Scaling Considerations

```
┌─────────────────────────────────────────────────────────────────┐
│                    HORIZONTAL SCALING                            │
└─────────────────────────────────────────────────────────────────┘

Single Instance:
   ┌────────────┐
   │ Socket.io  │
   │ Server     │
   │ + Handlers │
   └────────────┘

Multiple Instances (with Redis):
   ┌────────────┐      ┌────────────┐      ┌────────────┐
   │ Socket.io  │      │ Socket.io  │      │ Socket.io  │
   │ Server 1   │◄────►│ Server 2   │◄────►│ Server 3   │
   │ + Handlers │      │ + Handlers │      │ + Handlers │
   └─────┬──────┘      └─────┬──────┘      └─────┬──────┘
         │                   │                   │
         └───────────────────┴───────────────────┘
                             │
                             ▼
                     ┌───────────────┐
                     │ Redis Adapter │
                     │ (Pub/Sub)     │
                     └───────────────┘

   • Handlers work independently on each server
   • Redis adapter syncs room broadcasts
   • State is server-local (no distributed state)
   • Sticky sessions not required (connection recovery handles it)
```

## File Organization

```
src/
├── configs/
│   └── socket.js                    # Main socket configuration
│                                    # - Initializes handlers
│                                    # - Exports getters
│                                    # - Graceful shutdown
│
├── sockets/                         # Handler modules
│   ├── ticketCollision.socket.js   # Collision detection handler
│   ├── timeline.socket.js          # Timeline updates handler
│   │
│   ├── README.md                   # Architecture & API docs
│   ├── INTEGRATION_EXAMPLE.md      # Integration examples
│   ├── QUICKSTART.md               # Quick start guide
│   ├── IMPLEMENTATION_SUMMARY.md   # Implementation overview
│   └── ARCHITECTURE.md             # This file
│
└── services/
    ├── crmActivity.service.js      # Uses getTimelineHandler()
    ├── case.service.js             # Uses getTimelineHandler()
    ├── invoice.service.js          # Uses getTimelineHandler()
    └── omnichannelInbox.service.js # Uses getTicketCollisionHandler()
```
