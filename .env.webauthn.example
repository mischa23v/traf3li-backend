# ============================================
# WEBAUTHN/FIDO2 CONFIGURATION
# ============================================
# WebAuthn is used for hardware security keys and biometric authentication

# Relying Party Name - Your application name
WEBAUTHN_RP_NAME=Traf3li

# Relying Party ID - Your domain (without protocol, port, or path)
# IMPORTANT: Must match the domain where your frontend is hosted
# Production examples:
#   - WEBAUTHN_RP_ID=traf3li.com
#   - WEBAUTHN_RP_ID=dashboard.traf3li.com
# Development:
#   - WEBAUTHN_RP_ID=localhost
WEBAUTHN_RP_ID=localhost

# Origin - Full URL of your frontend (protocol + domain + port)
# IMPORTANT: Must match the exact origin where authentication requests come from
# Production examples:
#   - WEBAUTHN_ORIGIN=https://dashboard.traf3li.com
#   - WEBAUTHN_ORIGIN=https://app.traf3li.com
# Development:
#   - WEBAUTHN_ORIGIN=http://localhost:3000
#   - WEBAUTHN_ORIGIN=http://localhost:5173
WEBAUTHN_ORIGIN=http://localhost:3000

# ============================================
# CONFIGURATION NOTES
# ============================================
#
# 1. HTTPS is REQUIRED in production (except for localhost)
#
# 2. RP_ID must be:
#    - A valid domain or subdomain
#    - WITHOUT protocol (http/https)
#    - WITHOUT port
#    - WITHOUT path
#
# 3. ORIGIN must be:
#    - The complete URL including protocol
#    - Include port if non-standard (not 80/443)
#    - Must exactly match where your frontend is hosted
#
# 4. For subdomain deployments:
#    If your frontend is at https://dashboard.example.com
#    - WEBAUTHN_RP_ID=dashboard.example.com
#    - WEBAUTHN_ORIGIN=https://dashboard.example.com
#
# 5. For multiple subdomains (shared credentials):
#    Set RP_ID to parent domain (requires all subdomains to use same RP_ID)
#    - WEBAUTHN_RP_ID=example.com
#    - WEBAUTHN_ORIGIN=https://dashboard.example.com (specific subdomain)
#
# ============================================
# TROUBLESHOOTING
# ============================================
#
# Error: "Origin mismatch"
# Solution: Ensure WEBAUTHN_ORIGIN exactly matches your frontend URL
#
# Error: "RP ID mismatch"
# Solution: Ensure WEBAUTHN_RP_ID matches your domain (without protocol)
#
# Error: "NotAllowedError"
# Solution: Check that you're using HTTPS in production (or localhost in dev)
#
# ============================================
